# Database — PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/bts_teklif?schema=public"

# Authentication — Secret key for signing JWT tokens (change in production!)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Cron — Secret token for authenticating scheduled cron job requests
CRON_SECRET="your-cron-secret-change-in-production"

# App — Public URL of the application
NEXT_PUBLIC_APP_URL="http://localhost:3000"
