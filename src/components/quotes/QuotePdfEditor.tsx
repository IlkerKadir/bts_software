/**
 * Thin re-export so existing imports (`@/components/quotes/QuotePdfEditor`)
 * keep working after the editor was split into the `pdf-editor/` subfolder.
 */
export { QuotePdfEditor } from './pdf-editor/QuotePdfEditor';
