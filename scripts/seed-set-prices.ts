// ServiceSetPrice model has been removed from the schema.
// This script is no longer functional.

async function main() {
  console.log('ServiceSetPrice model was removed. Nothing to seed.');
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  });
